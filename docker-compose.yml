

version: "3.9"

services:
  frontend:
    build: ./frontend
    image: your-dockerhub-username/frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    image: your-dockerhub-username/backend:latest
    ports:
      - "8080:8080"
